/**
 * This file is not necessary, but it's good practice so that we don't need to type the full database name in the future.
 * Instead, we can just type DB_NAME when needed
 */
export const DB_NAME = "intro-to-backend";
